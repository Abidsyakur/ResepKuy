<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cookpad Indonesia</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            color:black;
            background-color: #f8f9fa;
        }
       .jumbotron {
            background-color: #fff;
            padding: 60px;
            margin-bottom: 30px;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
       .card {
            margin-bottom: 20px;
            border: none;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            background-color: #fff;
        }
       .card-img-top {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
       .card-body {
            padding: 20px;
        }
       .card-title {
            font-size: 24px;
            margin-bottom: 10px;
            color: #4a4a4a;
        }
       .card-text {
            font-size: 16px;
            margin-bottom: 20px;
            color: #6c757d;
        }
       .btn-primary {
            background-color: #F4C524;
            border-color: #F4C524;
        }
       .btn-primary:hover {
            background-color: #0069d9;
            border-color: #0062cc;
        }
       .logo {
            width: 70px;
            height: 70px;
            margin-right: 10px;
        }
           .footer {
            background-color: #fff;
            color: black;
            text-align: center;
            padding: 20px 0;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);

        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="my-4"><img src="logo.png" alt="Logo" class="logo">Resep Kuy</h1>
        <div class="jumbotron">
            <p>Temukan dan bagikan resep - resep favoritmu dengan komunitas kami.</p>
            <form class="my-4" id="search-form">
                <div class="input-group">
                    <input type="text" id="search-input" name="search-input" class="form-control" placeholder="Cari resep..." >
                    <button type="submit" id="search-button" class="btn btn-primary">Cari</button>
                </div>
            </form>
            <p>Atau, <a href="#">periksa kategori kami</a> untuk mendapatkan inspirasi.</p>
        </div>

            <div class="container-fluid py-sm-5">
            <div class="row">
                <div class="col-md-6">
                    <img src="homepage.jpg" class="img-fluid" alt="Foto Resep">
                </div>
                <div class="col-md-6">
                    <h2>Tentang Komunitas Resep Kuy</h2>
                    <p>
                        Resep Kuy adalah platform komunitas untuk berbagi resep dan inspirasi memasak. Bergabunglah dengan kami untuk menemukan resep-resep baru dan terbaik dari para koki amatir hingga profesional.
                    </p>
                </div>
            </div>
        </div>

    </div>
        <footer class="footer">
        <div class="container">
            <p>&copy; 2024 ResepKuy - All Rights Reserved</p>
        </div>
    </footer>

  <script>
    document.getElementById('search-form').addEventListener('submit', function(event) {
        event.preventDefault(); // Mencegah pengiriman formulir secara default

        // Ambil nilai input pencarian
        const searchTerm = document.getElementById('search-input').value;

        // Lakukan permintaan AJAX
        const xhr = new XMLHttpRequest();
        xhr.onreadystatechange = function() {
            if (xhr.readyState === XMLHttpRequest.DONE) {
                if (xhr.status === 200) {
                    // Tangani respon dari server
                    const response = JSON.parse(xhr.responseText);
                    // Misalnya, update konten halaman dengan hasil pencarian
                    updateSearchResults(response);
                } else {
                    console.error('Permintaan pencarian gagal');
                }
            }
        };

        // Buat URL pencarian sesuai dengan nilai input
        const searchUrl = `,/beranda?query=${encodeURIComponent(searchTerm)}`;

        // Kirim permintaan GET ke URL pencarian
        xhr.open('GET', searchUrl);
        xhr.send();
    });

    // Fungsi untuk memperbarui konten halaman dengan hasil pencarian
    function updateSearchResults(results) {
        // Misalnya, dapatkan elemen yang akan diperbarui, seperti div dengan ID 'search-results'
        const searchResultsElement = document.getElementById('search-results');

        // Bersihkan konten yang ada sebelum menambahkan hasil pencarian baru
        searchResultsElement.innerHTML = '';

        // Tambahkan hasil pencarian baru ke dalam elemen
        results.forEach(function(result) {
            const resultItem = document.createElement('div');
            resultItem.textContent = result.title; // Misalnya, gunakan judul dari hasil pencarian
            searchResultsElement.appendChild(resultItem);
        });
    }
</script>

</body>
</html>